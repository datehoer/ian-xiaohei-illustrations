interface:
  display_name: "口播手绘配图 · 柯基版"
  short_description: "设计可替换主角与中文口播分镜，生成带解释性引线标注的手绘配图"
  default_prompt: "Use $ian-xiaohei-illustrations 为这段口播生成一组配图，沿用极简线描柯基，并添加必要的解释性引线标注。"
policy:
  allow_implicit_invocation: true
