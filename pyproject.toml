[project]
name = "ian-xiaohei-video"
version = "0.1.0"
description = "Local narration, subtitle alignment and video composition for illustrated explainers"
requires-python = ">=3.11,<3.14"
dependencies = [
    "Pillow>=10,<13",
    "faster-whisper>=1.1,<2",
]

[tool.uv]
package = false
